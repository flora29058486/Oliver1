const jspObjItem = (props) => {
	return (
		<div>
			<p>{props.mode}</p>
		</div>
	)
}

const JspObjPanel = (props) => {

	const jspObj = props.jspObj;

	return (
		<div>

		</div>
	)
}

export default JspObjPanel;